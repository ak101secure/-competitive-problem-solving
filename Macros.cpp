#include <bits/stdc++.h>
using namespace std;
#define pb push_back

int main() {
    vector<int>vect ;
    vect.push_back(543);
    vect.push_back(434343);
    vect.push_back(332344);
    vect.push_back(333);

    for(auto x : v){
        cout<<x<<endl;
    }

    return 0;
}
